
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}


model User {
 Id String @id @map("_id") @default(auto()) @db.ObjectId
 Name String?
 Email String @unique
 Password String?
}